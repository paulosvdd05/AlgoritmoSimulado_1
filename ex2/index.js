const calcular = () =>{
    let produto = (document.getElementById('produto')).value
    let preco = (document.getElementById('preco')).value
    const promocao = preco*0.5

    div.innerHTML = `${produto} - Valor unitario`
}